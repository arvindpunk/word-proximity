import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arvindpunk.android_01',
  appName: 'wordstreetbets',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
